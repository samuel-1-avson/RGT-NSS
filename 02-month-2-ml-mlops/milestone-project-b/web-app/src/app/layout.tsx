import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Predictive Churn Analytics | Enterprise Solutions",
  description: "Advanced machine learning platform for customer churn prediction and retention management.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased bg-[#f8fafc]`}>
        {children}
      </body>
    </html>
  );
}
